name: final_moviles
description: "Aplicación de primeros auxilios y navegación en desastres."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  get: ^4.6.6
  camera: ^0.11.0+2
  permission_handler: ^11.3.1
  http: ^1.2.2
  speech_to_text: ^7.0.0
  flutter_tts: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
